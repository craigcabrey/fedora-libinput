---
- hosts: localhost
  roles:
  - role: standard-test-beakerlib
    tags:
    - classic
    tests:
    - libinput-tool-beakerlib

- hosts: localhost
  roles:
  - role: standard-test-basic
    tags:
    - classic
    tests:
    - libinput-tool
